{
  "compilerOptions": {
    "target": "ESNext",
    "module": "ESNext",
    "jsx": "react-jsx",
    "moduleResolution": "node",
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"],
      "@hooks/*": ["src/hooks/*"]  // 👈 Añade esto específicamente para hooks
    },
    "checkJs": true,  // 👈 Habilita verificación de JS
    "strict": false   // 👈 Mantén false si no quieres TypeScript-like checks
  },
  "include": [
    "src/**/*",
    "vite.config.js"  // 👈 Incluye archivos de configuración
  ],
  "exclude": ["node_modules"]
}